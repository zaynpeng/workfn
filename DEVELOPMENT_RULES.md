# Development Rules

## 单一职责

每个 Skill 只解决一个明确问题。不得通过改名重复创建职责相同的 Skill。

## 先诊断后输出

先检查参数、证据、风险和边界，再决定是否生成结果。

## 不强行生成

以下情况允许不生成成品：

1. 信息不足
2. 事实未确认
3. 需要内部确认
4. 当前不应回复
5. 当前不应跟进
6. 当前不值得继续投入

## 证据优先

业务判断按以下顺序优先使用证据：

1. 成交记录
2. 订单记录
3. 付款记录
4. 发货记录
5. 售后记录
6. 询价记录
7. 报价记录
8. 有效沟通记录
9. Obsidian 客户记录
10. Excel 已有备注
11. 官网信息
12. LinkedIn 入口
13. AI 推测

AI 推测只能作为待确认线索，不能替代事实。

## 不覆盖人工判断

AI 不得覆盖已经明确确认的人工事实和判断。

## 不把推测写成事实

任何不确定信息必须明确标注，并在需要时请求人工确认。

## 最小可用版本

先完成一个可测试、可审查的 Skill，再考虑扩展。

## 不追求全量完整

未判断、未确认和缺少证据的内容可以保留空白，不为形式完整而虚构内容。

# Skill 路由与编排原则

1. 单 Skill 优先
2. 多 Skill 必须存在真实依赖
3. 调用链第一版最多 5 个 Skill
4. 每一步必须有明确输入和输出
5. 必须设置停止条件
6. ROUTE() 不直接完成业务分析
7. 参数传递只保留必要字段
8. 待验证信息不得升级为事实

# Skill 命名规范

1. 所有正式 Skill ID 使用 `zayn-` 前缀
2. Skill ID 全部小写
3. 多词使用连字符
4. 展示名称继续保留函数式大写格式
5. 中文名称用于解释用途
6. 每个 Skill 的末级文件夹名称必须与正式 Skill ID 完全一致
7. 不使用他人的品牌前缀
8. 每个正式 `SKILL.md` 必须以 YAML front matter 开头
9. YAML front matter 必须包含唯一的 `name` 和一句完整中文 `description`
10. `description` 必须说明解决的问题、适用场景和核心输出，不得复制正文或宣传未实现能力

# Skill 统一运行协议

所有 Skill 必须遵守 [SKILL_RUNTIME_PROTOCOL.md](SKILL_RUNTIME_PROTOCOL.md)。

核心要求：

1. 先解析参数
2. 再检查完整度
3. 不足时要求补充
4. 不自动猜测
5. 达到最低运行条件后才正式分析
