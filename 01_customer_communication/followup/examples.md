# Examples

## 使用说明

本文件用于保存经过人工确认的真实案例或脱敏案例。
禁止把未经确认的推测写成事实。

当前尚未加入真实案例。以下调用模板不是案例，也不代表任何确定业务事实。

## 调用模板

```text
请调用 FOLLOWUP()。

客户：
[客户名称或类型]

历史合作：
[成交、订单、付款、发货、售后记录]

当前项目或询价：
[填写]

上次联系时间和内容：
[填写]

客户最后回复：
[填写]

已经跟进次数：
[填写]

本次可以提供的新价值：
[填写]

客户明确时间节点：
[填写]

我本次跟进目标：
[填写]

沟通渠道和语言：
[填写]
```

## 正常案例

等待加入经过人工确认的真实案例或脱敏案例。

## 信息不足案例

等待加入经过人工确认的案例。案例应验证缺少上次联系、历史成交、明确询价、新价值、时间节点或跟进次数时，Skill 是否降低投入并保留未判断。

## 风险案例

等待加入经过人工确认的案例。案例应覆盖无新价值连续催促、忽略客户时间节点、把浏览行为当采购意向、低证据高投入或缺少停止条件。

## 不应调用案例

等待加入经过人工确认的案例。应覆盖即时回复、询价价值判断、正式报价检查、客诉、重点客户长期战略、明确拒绝且无新信息以及群发开发信。

## v0.2 场景案例

以下为规则测试场景，不是真实业务事实。

### 案例一：信息完整，可正式运行

已提供客户背景、上次沟通内容和时间、当前卡点及跟进目标，并有新的库存信息可提供。预期判断是否跟进、推荐时间和一个主要动作。

### 案例二：关键参数缺失，应停止

只说“客户很久没回复”，没有上次沟通内容、时间和待推进事项。预期列出缺失项并停止正式分析。

### 案例三：输入存在冲突

沟通记录显示客户要求下月联系，但用户备注写“本周催促”。预期保留冲突，优先请求人工确认，不立即生成跟进内容。

### 案例四：需要转入相邻 Skill

客户最后一条消息含义不清。预期先转入 `INTENT_DECODE()`，明确表达和待确认事项后再回到 `FOLLOWUP()`。
