# Examples

## 使用说明

本文件用于保存经过人工确认的真实案例或脱敏案例。
禁止把未经确认的推测写成事实。

当前尚未加入真实案例。以下调用模板不是案例，也不代表任何确定业务事实。

## 调用模板

```text
请调用 REPLY()。

客户原话：
[粘贴客户消息]

当前已确认事实：
[填写]

尚未确认的信息：
[填写]

我的回复目标：
[填写]

希望客户下一步：
[填写]

沟通渠道：
[Email、WhatsApp、WeChat 等]

语言和语气：
[填写]

我原本准备的回复：
[可选]
```

## 正常案例

等待加入经过人工确认的真实案例或脱敏案例。

## 信息不足案例

等待加入经过人工确认的案例。案例应验证 Skill 是否拒绝自动补全事实，并能说明缺失信息、确认对象和临时回复边界。

## 风险案例

等待加入经过人工确认的案例。案例应覆盖过早承诺、证据不足、内部问题暴露、责任边界或缺少下一步等风险。

## 不应调用案例

等待加入经过人工确认的案例。应覆盖主动开发、跟进判断、询价价值判断、完整报价检查、客诉责任认定和内部汇报等非 REPLY() 场景。

## v0.2 场景案例

以下为规则测试场景，不是真实业务事实。

### 案例一：信息完整，可正式运行

客户原话、已确认事实、回复目标、不可承诺边界、渠道和语言均明确。预期先输出参数状态表，再进入正式模式并生成正式版与简洁版。

### 案例二：关键参数缺失，应停止

只提供客户原话，未说明回复目标和不可承诺边界。预期列出缺失项并停止，不生成可直接发送的回复。

### 案例三：输入存在冲突

库存表显示无货，但口头信息称有现货。预期保留两种来源并标记冲突，不对客户承诺库存。

### 案例四：需要转入相邻 Skill

用户要求回复客户的压价消息，但尚未确认价格策略。预期先转入 `PRICE()` 或 `NEGOTIATE()`，确认后再回到 `REPLY()`。
