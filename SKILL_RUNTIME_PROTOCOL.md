# WorkFn Skill Runtime Protocol

> WorkFn Skill 统一参数解析与运行协议

## 1. 协议目的

本协议用于统一所有 WorkFn Skill 的运行方式。

每个 Skill 都必须先判断：

```text
用户当前提供的信息，是否足以支持这个 Skill 做出可靠分析。
```

Skill 不得一开始直接生成答案。

## 2. 统一运行流程

所有 Skill 必须按以下顺序运行：

```text
用户输入
↓
输入解析
↓
参数映射
↓
参数状态表
↓
最低运行条件检查
↓
缺失或冲突参数处理
↓
用户补充
↓
再次校验
↓
正式分析
↓
结构化输出
```

不得跳过参数检查阶段。

## 3. 参数状态

所有 Skill 的参数状态只能使用以下五种：

```text
已命中
部分命中
缺失
冲突
待验证
```

### 已命中

用户已经提供足够清晰、可用于分析的信息。

### 部分命中

用户提供了一部分信息，但不足以支持完整判断。

### 缺失

用户没有提供该参数。

### 冲突

不同来源或用户前后表达存在不一致。

### 待验证

用户提供了信息，但其真实性、准确性或当前有效性尚未确认。

不得自行创造其他状态名称。

## 4. 必填参数与可选参数

每个 Skill 必须区分：

1. 必填参数
2. 建议参数
3. 可选参数

必填参数并不意味着全部必须一次性提供。

但每个 Skill 必须定义：

```text
最低运行条件
```

只有达到最低运行条件，才可以进入正式分析。

## 5. 最低运行条件

每个 Skill 必须明确说明：

1. 至少需要哪些参数
2. 哪些参数缺失时不能继续
3. 哪些参数缺失时可以先做初步分析
4. 哪些参数缺失只会降低完整度
5. 什么情况下必须停下来要求补充

最低运行条件不得由 AI 临时猜测。

## 6. 参数状态表

所有 Skill 在正式分析前，必须生成参数状态表。

统一格式：

| 参数 | 必需程度 | 当前状态 | 已获取内容 | 缺失或冲突影响 |
|---|---|---|---|---|
| 示例参数 | 必填 | 已命中 | 用户已提供内容 | 无 |
| 示例参数 | 建议 | 部分命中 | 已知部分信息 | 可能影响判断 |
| 示例参数 | 必填 | 缺失 | 无 | 无法进入正式分析 |

参数状态表必须基于用户输入，不得凭空补充。

## 7. 参数映射规则

Skill 应将用户自然语言映射为参数。

例如用户可能不会直接写：

```text
回复目标：争取客户确认替代方案
```

而可能写：

```text
我想先看看客户是不是愿意接受翻新的。
```

Skill 可以将其映射为：

```text
reply_goal = 确认客户是否接受翻新方案
```

但不得扩展为用户没有表达的目标。

## 8. 缺失参数处理

当参数缺失时，不得只回复：

```text
请补充更多信息。
```

必须具体说明：

1. 缺失哪个参数
2. 为什么重要
3. 会影响什么判断
4. 用户可以如何补充
5. 当前是否可以先做初步分析

推荐格式：

```text
当前缺少“本次回复目标”。

这个参数会影响回复是以确认需求、守住价格、争取反馈，还是暂缓承诺为主。

请补充一句：
“我希望客户下一步做什么？”
```

## 9. 冲突参数处理

当信息冲突时，必须：

1. 保留不同来源
2. 标记冲突
3. 不自行选择其中一个
4. 说明冲突会影响什么
5. 建议如何验证
6. 在冲突解决前降低分析置信度

示例：

```text
客户原话显示需要整机，但内部记录写的是平台。

当前无法确认最终交付范围。
建议先确认客户需要平台、机头还是整机。
```

## 10. 待验证参数处理

待验证信息必须明确标记。

例如：

1. 供应商口头反馈
2. 市场价格
3. 预计库存
4. 预计交期
5. 客户可能意图
6. 官网信息
7. LinkedIn 信息
8. AI 推测

不得把待验证信息写成确定事实。

## 11. 正式分析触发条件

只有同时满足以下条件，Skill 才可进入正式分析：

1. 已达到最低运行条件
2. 必填参数没有关键缺失
3. 关键冲突已解决，或已明确保留冲突
4. 主要证据来源已识别
5. 当前分析目标明确
6. 风险边界可以判断

未满足时，应停留在参数补充阶段。

## 12. 初步分析与正式分析

Skill 可以提供两种模式。

### 初步分析

适用于：

1. 参数不完整
2. 用户希望先看方向
3. 当前只能做低置信度判断

必须明确标注：

```text
以下为初步分析，仍需补充关键参数。
```

### 正式分析

适用于：

1. 达到最低运行条件
2. 参数状态足够稳定
3. 关键风险已可判断

不得把初步分析包装成确定结论。

## 13. 输出置信度

如输出涉及推断，可使用：

```text
高
中
低
```

不得输出伪精确百分比，除非该 Skill 有明确评分模型和证据依据。

置信度必须说明原因。

## 14. 正式输出前检查

进入正式输出前，必须检查：

1. 是否有参数缺失
2. 是否有参数冲突
3. 是否有待验证信息
4. 是否把推测写成事实
5. 是否覆盖人工判断
6. 是否越过 Skill 职责边界
7. 是否强行生成结果
8. 是否遗漏用户真正目标

## 15. 禁止事项

所有 Skill 均不得：

1. 跳过参数解析
2. 直接进入答案生成
3. 自动补全缺失参数
4. 把可能性写成事实
5. 用低等级证据覆盖高等级证据
6. 在参数冲突时自行选边
7. 为了完整而强行输出
8. 把用户未表达的目标写进去
9. 覆盖人工确认内容
10. 因为模板要求而填满所有字段
11. 伪造置信度
12. 把平台信号当作真实业务证据

## 16. 推荐统一输出结构

所有 Skill 在分析前可使用：

### A. 参数完整度结论

```text
可进入正式分析
可以先做初步分析
需要补充关键参数
存在冲突，暂不建议继续
```

### B. 参数状态表

使用统一表格。

### C. 需要补充的信息

按优先级列出。

### D. 当前可执行动作

明确下一步只做什么。

### E. 正式分析

只有达到触发条件时才出现。

## 17. 人工判断边界

1. 用户明确确认的事实优先
2. 人工判断不得被自动覆盖
3. AI 可以指出风险，但不能擅自修改结论
4. AI 不得修改原始数据
5. AI 不得将疑似信息写入确定字段
6. AI 输出应保留人工复核入口

## 18. 适用范围

本协议适用于所有 WorkFn Skill，包括：

1. 客户沟通类
2. 询价与报价类
3. 订单与交付类
4. 客诉与售后类
5. 客户管理类
6. 内部协同类
7. 个人效率类
8. 平台专用类
9. 产品理解类

