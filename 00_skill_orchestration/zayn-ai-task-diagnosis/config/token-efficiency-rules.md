# Token 与资源效率

评估重复读取、重复搜索、全量分析、模型调用次数、数据范围和模型能力。优先：复用历史输出；建立索引/摘要；只处理新增数据；先处理高价值样本；机械步骤改本地程序；稳定规则写入 Skill；按需触发；避免简单任务使用高能力模型。

输出主要消耗点、无效调用、程序化步骤、保留 AI 的判断、推荐频率、数据范围、最低模型能力、缓存/索引/摘要需求。

省 Token 不是单纯缩短提示词，而是减少无效任务、重复读取、重复搜索、全量分析和不必要的 AI 判断。
