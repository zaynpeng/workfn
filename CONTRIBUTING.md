# Contributing

WorkFn 当前主要维护 Markdown Skill 规范，请保持贡献轻量、可审查。

## 基本要求

1. 不接受仅改名、职责重复的 Skill。
2. 新 Skill 必须只有一个明确职责。
3. 必须说明适用场景和不适用场景。
4. 必须说明输入参数、证据要求和风险边界。
5. 不得把未经确认的业务推测写成正式规则。
6. 提交前必须提供测试案例。
7. 当前阶段建议先提出 Issue 并确认职责边界，不直接进行大规模扩展。

## 文档检查

- 展示名称使用 `NAME()` 格式。
- 目录使用小写名称且不带括号。
- 每个 Skill 只包含五个标准文件。
- 示例必须真实、脱敏并经过人工确认，或明确标注为测试样例。
- 不得填写未经确认的作者、邮箱、网站、平台兼容性或许可证信息。
